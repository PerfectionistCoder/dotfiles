#!/usr/bin/env fish

test (count $argv) -eq 0; and exit 1

set -l command $argv[1]
set -l hostname (uname -n)

sudo nixos-rebuild $command --flake $HOME"/{{.dirs.nixos_config}}/"#$hostname --impure $argv[2..]
